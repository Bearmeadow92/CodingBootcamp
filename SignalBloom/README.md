# Signal Bloom

A simple Node + static site to showcase and sell photo prints with a blog section.

## Usage

```bash
npm install
npm start
```

The site serves:
- `/` – home page
- `/shop.html` – marketplace for prints
- `/blog.html` – blog posts

Product and blog data live in the `data` directory. Replace image URLs and add posts as needed.
